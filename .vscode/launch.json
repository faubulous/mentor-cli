{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Debug mentor-cli",
      "program": "${workspaceFolder}/out/mentor-cli.js",
      "args": [
        "generate",
        "--input",
        "test/*.ttl",
        "--create-index-ts",
        "--create-src-ts"
      ],
      "cwd": "${workspaceFolder}"
    }
  ]
}